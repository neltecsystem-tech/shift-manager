# 引き継ぎ (2026-07-03) — Mac での作業継続用

このセッションで **shift-manager / delivery-manager / kaikei-manager / pay-statement-viewer** を横断で触りました。
Mac 側では各リポジトリを `git pull` してから続けてください。**デプロイ方法がアプリごとに違う**ので下記に注意。

---

## 🚀 デプロイ方法（重要・アプリごとに違う）

| アプリ | ローカル | デプロイ方法 | 反映先 |
|---|---|---|---|
| **shift-manager** | `~/…/shift-manager` | **git push** → GitHub Pages | `https://neltecsystem-tech.github.io/shift-manager/` |
| **delivery-manager** | `~/…/delivery-manager` | ⚠️ **手動** `npm run build && npx wrangler deploy` | `https://delivery-manager.neltec.workers.dev/` |
| **kaikei-manager** | `~/…/kaikei-manager` | **git push** → Cloudflare 自動 | `https://kaikei-manager.neltec.workers.dev/` |
| **pay-statement-viewer** | `~/…/pay-statement-viewer` | **git push** → GitHub Pages | `https://neltecsystem-tech.github.io/pay-statement-viewer/` |

### ⚠️ ハマりどころ
- **delivery-manager は git push だけでは本番に出ない**。`.github/workflows` が無く、`npx wrangler deploy` が必須。git push は旧アカウント(yuta-kobayashi-59a)側に飛ぶ恐れ。`npx wrangler whoami` が **neltec.system@gmail.com** であること。
- **shift-manager / GitHub Pages は反映に 1〜2分ラグ**あり。push 直後に「反映されない」と見えるのは大抵これ。`curl -s https://neltecsystem-tech.github.io/shift-manager/index.html | grep -c "<目印文字列>"` で確認するとよい。
- **ブラウザキャッシュ**：確認時は必ず `Cmd+Shift+R`。

### Edge Function のデプロイ（Supabase PAT）
```
SUPABASE_ACCESS_TOKEN=sbp_… (Windows の ~/.claude memory「reference_supabase_pat」参照。無ければ再発行依頼) \
  npx supabase functions deploy <fn> --project-ref nccognptoprhwsbjnwcu --no-verify-jwt
```
- **EF の source があるリポジトリのルートから実行**すること（invoice-sheet/pay-sheet-sync は shift-manager or kaikei 側にある。パスを間違えると "Entrypoint path does not exist")。
- PAT: `sbp_… (Windows の ~/.claude memory「reference_supabase_pat」参照。無ければ再発行依頼)`（フルスコープ / 期限切れなら再発行依頼）

### 主要な参照値
- workchat/NexPort Supabase: **nccognptoprhwsbjnwcu**（shift の EF・kaikei の acc_ テーブル・単価/稼働シート）
- delivery-manager Supabase: **ymdaakiwxkjotlmzveom**
- 管理者PW / INV_PW: **neltec2026**
- Google Sheets: 人員名簿=`1yVKQLSmdc9RZ2U5m4CIPqAl4JqP9siiSeSRP0z3SEEM` / 店舗マスタ=`1KvMbiLMeUmUOiUwzcilp-6u2xzZXs68HcvtWuWqT70M` / 単価・稼働記録=`1fxzGdAE64wcFLL0_mZI-eFk6U5jxqCh7DtAIzinoymQ` / ヤマト売上=`1Zq0ISTbZSQFWVJmS-S9TypQZw_wKm-YZvz3G877iQ4o` / 川越コース単価マスタ=`1Owv83TGxSl15pqO0MaaF4AaLeslye0frfKAuo62TlGY`(マスタ2)

---

## 📋 このセッションでやったこと（アプリ別）

### shift-manager（最終コミット 54a7e9a）
- **稼働記録の二重計上防止**：同(スタッフ+日付+区分+コース)は登録/更新不可。client(invSaveWork)+EF(add_work/update_work、409 DUPLICATE)。共通キー `workDupKey`。
- **法人オーナー/会計担当のスタッフ名変更**：`invIsCorpOwner`→`invCanManageCompany` に拡張。
- **稼働登録の解禁時刻**：当日分は朝刊(庫内朝刊含む)=4:00 / 夕刊(競馬/庫内夕刊)=14:00 以降のみ。UTC→JST の"今日"バグも修正。client+EF(add_work、403 TOO_EARLY)。
- **シフトと区分ズレ警告 + 庫内をシフトから拾う**：`invShiftSlots`/`invSlotCat`/`invShiftExpectedCats`/`invCheckShiftCat`。
- **夜間再計算EF `recompute_amounts` を川越コース別自動単価に対応**（旧/新パターン）。デプロイ済。
- **コース名変更**：店舗マスタで 城北17→城北8、城北18→城北9（夕刊/競馬列 計159セル）。batch_update_cells で実施済。
- **重複稼働記録15件を削除**（古い方=row小を削除）。delete_workは行クリア方式。
- **スタッフ管理に営業所フィルタ**（最終形）：
  - 営業所ボタン（全て/城北/川越/立川/川崎高津/アスクル）。**既定=全て（必ず表示）**。
  - 営業所判定は **人員名簿(DB.staff) + 稼働記録(invWork のコース接頭辞 areaOfCourse) の二重ソース**。
  - **`get_roster` アクションを invoice-sheet EF に新設**（ROSTER_SHEET_ID から人員名簿全行）。loadLiveStaff を fetch-shift→get_roster に変更（fetch-shift のSSに人員名簿シートが無く失敗していた）。
  - **ログイン時に先読み**（単価/稼働/人員名簿）。
- **保存後の楽観的更新**（Sheets反映ラグで即反映されない対策）。

### delivery-manager（最終コミット 手動デプロイ済み）
- **売上シートに荷主切替ボタン**（ヤマト/SBS/東スポ）。ヤマトは外部シート、SBSは delivery_records の日×ドライバー集計。
- **東スポの売上 = 稼働カレンダー(tokyosp_shifts の稼働日数) × 日当(単価マスタ)**。
- **設定に単価マスタ**（荷主へ請求する単価 = `client_billing_rates` テーブル新設）。**ヤマトは売上シートから宅配完/ポス単価を逆算して自動入力**（`deriveYamatoUnitPrices`、宅配完¥180/ポス¥60を実証）。
- **支払明細でドライバー名クリック→詳細明細モーダル**。
- すべて `npx wrangler deploy` で本番反映済み・ライブ確認済み。

### kaikei-manager（最終コミット e02010e 付近）
- **支払計算書**：社員(正社員/役員)を一番上に。給与マスタ(acc_salaries)反映。社員は**売上(アスクル等)+固定給を1行**。マスタ荷主以外の売上は別行(売上のみ)。
- **社員の荷主**：acc_salaries に `shipper` 追加、給料マスタ画面で登録。「東京即売」で統一（営業所別に分けない）。営業所別売上表からは「東京即売(単独)」を除外。
- **支払サイトのマスタ化** `acc_payment_sites`（6月から137件初期投入）。自動入力で反映、保存でマスタ更新。設定に「📅 支払サイトマスタ(取引先)」画面追加。
- **自動更新機構**（`src/lib/autoUpdate.ts`）：新デプロイ検知でバナー/自動リロード。
- **pay-sheet-sync EF v13**（社員の荷主を返す）デプロイ済。

### pay-statement-viewer
- メンバー管理一覧に「📄 明細」ボタン（管理者が各メンバーの支払明細をワンクリック表示）。

---

## ✅ 次にやること / 要確認
1. **shift-manager スタッフ管理**：最新デプロイ反映済み(get_roster/既定=全て)。Mac でも `Cmd+Shift+R`→スタッフが全員出て、営業所ボタンで絞れるか確認。もし出なければ画面のメッセージ(読み込み中/該当なし/取得できません)を確認。
2. **fetch-shift EF は別管理**（source が手元に無い）。人員名簿は get_roster で代替済みなので急ぎではないが、シフト取込で人員名簿を使う箇所があれば要注意。
3. delivery-manager の SBS 単価マスタは空。SBSの請求単位(件/日当)が決まれば設定に入力。
4. kaikei の請求書計算に単価マスタを差し込む配線は未実施（保管のみ）。必要なら対応。

---

## メモ
- Windows 側の Claude メモリ（`~/.claude/projects/.../memory/`）に各プロジェクトの詳細メモあり。Mac 側の Claude には引き継がれないので、この HANDOFF と各リポジトリの既存 memory/HANDOFF を参照。
- 全体的に「push しただけ＝本番未反映」を疑うこと（特に delivery-manager 手動 / GitHub Pages ラグ）。
